import ActionsConst from "../actionConstatns/actionConstans";
import update from "react-addons-update";

const product = (store=null, action) =>{
    switch (action.type) {

    }
    return store;
}

export default product;